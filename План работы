Main:
	проветка в мейне на тип полученного файла, генерация генерация исключения о том,
	что полученный файл имеет не тот формати, его обработка с выводом ошибки
	считывание файла
	парсер<какой парсер использовать для парсинга маркдавн>(в данном случае будет использоваться парсер хтмл)
	парсинг
	запись полученного хтмл файла

Классы и интерфейсы:
	класс конвертер из маркдавн в некий формат,
	
	класс объектного дерева вложений,
	
	интерфейс объектов,
	
	классы наследуемые от интерфейса объектов:
  	<p> - двух переводов строки, между которыми нет ничего кроме пробельных символов,
  	<em> - Текст окруженный с двух сторон одинарными символами подчерка,
  	<strong> - текст окруженный с двух сторон двойными символами подчерка,
  	<code> - Текст окруженный одинарными _обратными_ кавычками (backticks)
	
	класс парсер маркдавн в объектный код, который будет проходить по всему маркдавн тексту и
	формировать из него объектное дерево вложений,
	
	интерфейс парсера объектного дерева в некий формат(для того, чтобы в дальнейшем можно было расширить функционал -
	сформировать новый класс на основе интерфейса для парсинга в некий формат),
	
	класс парсер объектного дерева в хтмл унаследованый от интерфейса парсера объектного дерева,
	который будет проходить по объектному коду и формировать из него хтмл разметку,

	*экранирование,
	*подчерк с цифрой - не должен считаться выделением,
	*непарные не работают
